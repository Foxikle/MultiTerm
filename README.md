# Operations

- **touch** Create a new file (touch file.txt)
- **cat** Read a file (cat file.txt)
- **rm** delete a file (rm file.txt)
- **tee** Write to a file ("Text to write" > file.txt)
- **help** Prints out a message like this
